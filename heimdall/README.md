# Heimdall Dashboard

## Prerequisites

- Install Docker as per operating system requirements e.g. [Linux Installation guide](https://pimylifeup.com/linux-docker-install/)
- Create supporting directories for Heimdall stack and config files: `mkdir -p /opt/stacks/heimdall`
- Create `.env` and set `HEIMDALL_BIND_IP=<IP>`
